version: 1.0
runtime: python3.11
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: streamlit run app.py --server.port 8080 --server.headless true
  network:
    port: 8080